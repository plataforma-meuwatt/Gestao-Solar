[pytest]
testpaths = tests
# `auto`: um `async def test_…` roda sem marcador. O envio e o processamento do webhook são
# assíncronos, e um teste async SEM marcador não falha — ele é pulado com um aviso fácil de
# não ler, que é a pior forma de um teste "passar".
asyncio_mode = auto
